# qt-robot-controller
Modern Qt-based distributed robot control system with dynamic network discovery. Qt desktop app (PC) connects to simple Python script (Raspberry Pi) for seamless robot control over local network.
