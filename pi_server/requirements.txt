# Networking (Core - Required)
websockets>=12.0
aiohttp>=3.9.0
python-socketio[server]>=5.10.0

# GPIO & Hardware (Core - Required)
RPi.GPIO>=0.7.0
gpiozero>=2.0
pigpio>=1.78

# Camera (Core - Required for video streaming)
opencv-python>=4.8.0
# picamera2>=0.3.0  # Pre-installed on Raspberry Pi OS, no pip install needed

# Utilities (Core - Required)
pyyaml>=6.0
numpy>=1.24.0
colorama>=0.4.6
Pillow>=10.0.0

# Sensors (Optional - Install only if you have the hardware)
# Uncomment the lines below if you have these sensors:

# IMU Sensor (MPU6050)
# adafruit-circuitpython-mpu6050>=1.3.0  # Note: Latest is 1.3.5, not 2.0.0

# Ultrasonic Sensor (HC-SR04)
# adafruit-circuitpython-hcsr04>=0.4.0

# LIDAR (RP-LIDAR A1)
# rplidar>=0.9.2

# OLED Display (SSD1306)
# luma.oled>=3.12.0

# Audio (Optional - only if using sound)
# PyAudio>=0.2.14
